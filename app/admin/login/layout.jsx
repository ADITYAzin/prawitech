export const metadata = {
  title: "Admin Portal | Prawitech",
  description: "Secure login for Prawitech administrators.",
};

export default function LoginLayout({ children }) {
  return children;
}